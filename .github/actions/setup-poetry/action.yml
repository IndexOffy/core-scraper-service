name: 'Setup Poetry'
description: 'Setup Poetry package manager'
inputs:
  poetry-version:
    description: 'Poetry version'
    required: false
    default: 'latest'
  install-dependencies:
    description: 'Install dependencies groups (comma-separated)'
    required: false
    default: ''

runs:
  using: 'composite'
  steps:
    - name: 📦 Install Poetry
      uses: snok/install-poetry@v1
      with:
        version: ${{ inputs.poetry-version }}
        virtualenvs-create: true
        virtualenvs-in-project: true
        installer-parallel: true

    - name: 📚 Install dependencies
      shell: bash
      run: |
        GROUPS="${{ inputs.install-dependencies }}"
        if [ -z "$GROUPS" ]; then
          poetry install --no-interaction --no-root
        else
          WITH_ARGS=""
          for group in $(echo "$GROUPS" | tr ',' ' '); do
            group=$(echo "$group" | xargs)
            [ -n "$group" ] && WITH_ARGS="$WITH_ARGS --with $group"
          done
          poetry install --no-interaction --no-root $WITH_ARGS
        fi
